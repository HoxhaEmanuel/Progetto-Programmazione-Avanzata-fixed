# Configurazione del Database per Test Locali
DB_NAME=test_progetto_crowdsourcing
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_DIALECT=postgres

# Configurazione JWT
JWT_SECRET=test_jwt_secret_key

# Configurazione del Server
PORT=3001
NODE_ENV=test

# Note per Test:
# 1. DB_HOST è impostato su 'localhost' per i test locali
# 2. Porta diversa (3001) per evitare conflitti
# 3. Database di test separato
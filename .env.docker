# Configurazione del Database per Docker
DB_NAME=crowdsourcing_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=postgres
DB_PORT=5432
DB_DIALECT=postgres

# Configurazione JWT
JWT_SECRET=your_super_secret_jwt_key_here_change_this_in_production

# Configurazione del Server
PORT=3000
NODE_ENV=production

# Note per Docker:
# 1. DB_HOST è impostato su 'postgres' che è il nome del servizio nel docker-compose
# 2. Le porte sono quelle interne ai container
# 3. NODE_ENV è impostato su production per l'ambiente containerizzato